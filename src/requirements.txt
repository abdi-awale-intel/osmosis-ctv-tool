# Core dependencies for Osmosis CTV Tool
pandas>=1.3.0
numpy>=1.21.0
openpyxl>=3.0.0

# Optional dependencies for enhanced functionality
xlsxwriter>=3.0.0

# GUI dependencies (install separately if needed due to network issues)
# customtkinter>=5.2.0

# Other dependencies (install separately if needed)
# Pillow>=8.0.0
# python-dateutil>=2.8.0
# pytz>=2021.1
# PyInstaller>=5.0.0
